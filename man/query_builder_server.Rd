% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_builder.R
\name{query_builder_server}
\alias{query_builder_server}
\title{Create query builder server logic}
\usage{
query_builder_server(
  id,
  pool,
  selected_table_name,
  selected_tbl_ref_without_restricted_columns,
  where_clause,
  needs_summary,
  group_vars,
  summary_specs,
  banding_configs = NULL,
  regrouping_configs = NULL
)
}
\arguments{
\item{id}{Character. The module ID}

\item{pool}{Database connection pool}

\item{selected_table_name}{Reactive expression returning the selected table name as a character string}

\item{selected_tbl_ref_without_restricted_columns}{Reactive expression returning a tbl reference (dbplyr lazy table)}

\item{where_clause}{Reactive expression returning the WHERE clause as a character string}

\item{needs_summary}{Reactive expression returning a logical indicating if summarization is needed}

\item{group_vars}{Reactive expression returning a character vector of grouping variables}

\item{summary_specs}{Reactive expression returning a list of summary specifications}

\item{banding_configs}{Reactive expression returning a named list of banding configurations for numeric variables (optional)}

\item{regrouping_configs}{Reactive expression returning a named list of regrouping configurations for categorical variables (optional)}
}
\value{
A list with three reactive expressions:
\describe{
\item{query}{Reactive returning a dbplyr query object (lazy tbl) with all transformations applied, or NULL if no table selected}
\item{needs_summary}{Reactive returning the logical flag passed in (pass-through)}
\item{error}{Reactive returning error message string or NULL if no error}
}
}
\description{
Builds a complete dbplyr query by combining filters, grouping, transformations (banding/regrouping),
and summarization specifications.
}
\details{
The query is built in this order:
\enumerate{
\item Start with base table reference
\item Apply WHERE clause filters
\item Apply banding transformations (e.g., age ranges)
\item Apply regrouping transformations (e.g., combine categories)
\item Apply GROUP BY if needed
\item Apply summary functions (mean, sum, count, etc.)
}

All operations use dbplyr, so the query remains lazy until explicitly collected.
}
